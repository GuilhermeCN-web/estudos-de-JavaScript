const arr = [10, 8, 6, 4, 2, 0];
    console.log("Start loop!");
        for (let i = 0; i < arr.length; i++) {
            arr[i] /= 2;
        }
    console.log("A comparação está completa");
    console.log(arr);